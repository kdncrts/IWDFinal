extends layout

block content
    - var toppings = ["Pepperoni", "Mushroom", "Anchovies", "Sausage", "Artichokes", "Pineapple"];
    - var colors = ["Red", "Orange", "Yellow", "Green", "Blue", "Purple", "Pink", "Black", "White"];
    if error
        p(class="text-red")=error

    form(action="/register" method="post")
        p   
            | Username
            br
            input(type="text" name="username")
        p
            | Password
            br
            input(type="password" name="password")
        p
            | Confirm Password
            br
            input(type="password" name="confirmPassword")
        p
            | Email
            br
            input(type="text" name="email")
        p
            | Age
            br
            input(type="number" name="age")
        p
            | Favorite Pizza Topping
            input(checked type="radio" name="topping" value="none" style="visibility:hidden")
            each topping in toppings
                br
                input(type="radio" name="topping" value=topping.toLowerCase())
                label(for=topping)
                    = topping 
        p
            | Favorite Color
            input(checked type="radio" name="color" value="none" style="visibility:hidden")
            each color in colors
                br
                input(type="radio" name="color" value=color.toLowerCase())
                label(for=color)
                    = color 
        p
            | Random Number from 0-100
            br
            input(type="number" name="random" value=-1)
        input(type="submit")